//let fruit = ["apple", "orange", "water melon", "pumpkin", "plum", "pineapple", "banana", "lemon", ];
//let skaiciai = [1, 5, 3, 7, 14, 12, 42, 34, 16, 23, 45, 76, 9, 16, 9, 1, 3, 42, 16];

//console.log(fruit);
//console.log(fruit.push("grapes")); // add a item from the end
//console.log(fruit.pop(1)); // remove a item/s from the end
//console.log(fruit.shift(1)); // adds from the beggining
//console.log(fruit.unshift("grapes")); // adds an item from the beggining
//console.log(fruit.splice(2, 8, "grapes", "cherry")); // adds an item in a specific spot or removes it (a = the row, b = how many to keep, 0 means inactive, c = type in what to add)

//fruit.forEach((item, index) => {console.log(index+1, item)});

//let ats = fruit.map(elem => {console.log(elem);});

//let result = fruit.map(function(item, index){console.log(index+1, item);});

//let ieskau = fruit.filter(reikalingas => reikalingas=="apple"); // vieto == gali buti >, <, !=, etc...
//console.log(ieskau);

//console.log(fruit.sort());

//(arrayname.slice([start, [end]])) let's you copy a part of a array and splice it in to another or do something else 
//(arrayname.concat()) let's you create a new array that includes elements from other arrays and additional elements

//(arrayname.indexof('SearchElement', FromWhereToStart)) searches for the given element in the array and showes the index(row) of it the first time it spots it
//console.log(fruit.indexOf("plum")); //found it in 4th index (start counting from zero)
//console.log(fruit.indexOf("plum", 5)); // the defines at what index it begins the search (so if start it beyond 4 in this array you get -1 as the it thinks it doesn't exist) 

//(arrayname.includes('something')) does a check if the given element is included in the string (basicly what makes up the array) and gives either a true or false answear


//let vardas = ["Jonas", "Ieva", "Rugile", "Jokubas", "Simona", "Simona", "Deividas", "Lukas", "Rugile"];






